F8::
CheckTitleCur := "[RC] DETECTED"
if InStr(CheckTitleCur,"Problem loading page"){
	MsgBox, %CheckTitleCur%
}

;	F8::
;	SetFormat, float, 0.1
;	TABNUMLOCK := 0
;	TABNUMLOCK := TABNUMLOCK+0.5
;	MsgBox, %TABNUMLOCK%
;	TABNUMLOCK := TABNUMLOCK+0.5
;	MsgBox, %TABNUMLOCK%
;	TABNUMLOCK := TABNUMLOCK+10
;	MsgBox, %TABNUMLOCK%